"""CUDA backend (placeholder — not yet implemented).

To add: drop a `backend.py` here exposing the same render(...) / close()
interface as backends.tt.backend, plus a `kernels/` subdir with the .cu /
.cuh sources and any build script needed.
"""
